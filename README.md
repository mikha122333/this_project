# this_project
my first project to try git
git checkout -b "название ветки"-переход на ветку

git add . - сохр. изменения(все)

git reset "что-то" - убр. изм.

git commit -m "описание изменений"

git push origin "имя_ветки_"

git status