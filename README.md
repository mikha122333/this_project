# this_project
** my first project to try git **
```
git checkout -b "название ветки"
```
*переход на ветку
```
git add .
```
*сохр. изменения(все)
```
git reset "что-то"
```
*убрать изменения
```
git commit -m "описание изменений"
```
*коммит и подпись изменений
```
git push origin "имя_ветки_"
```
*то как запушить изменеия в нужную ветку
```
git status
```
*проверка статуса